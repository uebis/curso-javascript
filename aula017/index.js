/*
function saudacao(nome) {
    return `Bom dia, ${nome}!`;
}

const variavel = saudacao('Andrezza');
console.log(variavel);
*/

/*
function soma(x = 1, y = 2) { // posso declarar um valor padrão caso a pessoa não coloque nenhum valor na hora de chamar a função
    const resultado = x + y;
    return resultado; // nada que estiver abaixo de return será lido na função
}

console.log(soma(2, 2));
console.log(resultado); // vai gerar erro, pois resultado está dentro da função e não é possível acessá-lo de fora

const resultado = soma(2, 2); // consigo criar uma variável com o mesmo nome da que criei dentro da função
*/

/*
const raiz = function(n) {
    return n ** 0.5;
}; // aqui o ponto e vírgula é obrigatório

console.log(raiz(9));
*/

/*
const raiz = n => n ** 0.5; // arrow function 

console.log(raiz(9));
*/